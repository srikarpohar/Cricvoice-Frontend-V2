import { useState } from "react";

interface IProps{

}

interface IState{

}

export const SignupPage = (props: IProps) => {
	const [state, setState] = useState<IState>({});

	return (<div></div>);
}